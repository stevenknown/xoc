#NOTE: CFLAG doesn't include -no-prssa, because we use Classical-NonPR-DU-chain and PRSSA to test CP.
#NOTE: need prmode here.
CFLAG:-O3 -no-vect -no-if_cvs -dump-cp -prdu -nonprdu -no-mdssa -prmode -only-cp_aggr
